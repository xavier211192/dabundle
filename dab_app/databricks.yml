# This is a Databricks asset bundle definition for dab_app.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_app
  uuid: d93b43d4-aad5-4e21-83d8-98aadffee88e

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-32f25b25-f4e0.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-32f25b25-f4e0.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/prashanth.xavier21@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/prashanth.xavier21@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: prashanth.xavier21@gmail.com
        level: CAN_MANAGE
